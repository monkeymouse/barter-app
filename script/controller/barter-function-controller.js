define( [ "barterModule" ],
	function( barterModule ){
		barterModule.controller( "BarterFunctionController",
			function( ){
				console.debug( "Barter function controller loaded! ", $scope );
			} );
	} );