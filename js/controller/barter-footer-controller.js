define( [ "barterModule" ],
	function( barterModule ){
		barterModule.controller( "BarterFooterController",
			function( $scope ){
				console.debug( "Barter footer controller loaded! ", $scope );
			} );
	} );